{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "my-helm-test.serviceAccountName" . }}
  labels:
{{ include "my-helm-test.labels" . | indent 4 }}
{{- end -}}
