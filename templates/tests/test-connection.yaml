apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-helm-test.fullname" . }}-test-connection"
  labels:
{{ include "my-helm-test.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "my-helm-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
